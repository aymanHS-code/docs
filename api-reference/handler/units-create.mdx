---
title: "Create Unit"
api: "POST /units"
authMethod: none
openapi: "/openapi-specs/Roomi Handler.json POST /units"
---

Create a new unit.

## Headers


| Name | Required | Description |
| --- | --- | --- |
| Authorization | Yes | AWS4-HMAC-SHA256 signature |
| X-Amz-Content-Sha256 | Yes | SHA256 hash of the request body |
| X-Amz-Date | Yes | ISO8601 timestamp |


## Body

```json
{
  "unit_id": "unit-456",
  "unit_name": "Unit A-101",
  "lifesmart_id": "LS-123",
  "lifesmart_pass": "secret"
}
```


<RequestExample>

```json Request Body
{
  "created_at": "created_at",
  "updated_at": "updated_at",
  "unit_id": "unit_id",
  "unit_name": "unit_name",
  "lifesmart_id": "lifesmart_id",
  "lifesmart_pass": "lifesmart_pass"
}
```

</RequestExample>


<ResponseExample>

```json Response
{
  "created_at": "created_at",
  "updated_at": "updated_at",
  "unit_id": "unit_id",
  "unit_name": "unit_name",
  "lifesmart_id": "lifesmart_id",
  "lifesmart_pass": "lifesmart_pass"
}
```

</ResponseExample>


<!-- GeneratedFieldsUI:post_units -->
<Panel>
  <div id="form-post_units">
    <form id="f-post_units" style="display:block;max-width:720px">
      <label style="display:block;margin:6px 0 2px">created_at </label><input id="fld_created_at" type="text" placeholder="created_at" style="width:100%;padding:8px;border:1px solid #ddd;border-radius:6px">
<label style="display:block;margin:6px 0 2px">updated_at </label><input id="fld_updated_at" type="text" placeholder="updated_at" style="width:100%;padding:8px;border:1px solid #ddd;border-radius:6px">
<label style="display:block;margin:6px 0 2px">unit_id *</label><input id="fld_unit_id" type="text" placeholder="unit_id" style="width:100%;padding:8px;border:1px solid #ddd;border-radius:6px">
<label style="display:block;margin:6px 0 2px">unit_name *</label><input id="fld_unit_name" type="text" placeholder="unit_name" style="width:100%;padding:8px;border:1px solid #ddd;border-radius:6px">
<label style="display:block;margin:6px 0 2px">lifesmart_id *</label><input id="fld_lifesmart_id" type="text" placeholder="lifesmart_id" style="width:100%;padding:8px;border:1px solid #ddd;border-radius:6px">
<label style="display:block;margin:6px 0 2px">lifesmart_pass *</label><input id="fld_lifesmart_pass" type="text" placeholder="lifesmart_pass" style="width:100%;padding:8px;border:1px solid #ddd;border-radius:6px">
<label style="display:block;margin:6px 0 2px">lifesmart_usertoken (JSON)</label><textarea id="fld_lifesmart_usertoken_json" rows="4" placeholder="{}" style="width:100%;padding:8px;border:1px solid #ddd;border-radius:6px"></textarea>
<label style="display:block;margin:6px 0 2px">lifesmart_usertoken_expiry (JSON)</label><textarea id="fld_lifesmart_usertoken_expiry_json" rows="4" placeholder="{}" style="width:100%;padding:8px;border:1px solid #ddd;border-radius:6px"></textarea>
<label style="display:block;margin:6px 0 2px">metadata (JSON)</label><textarea id="fld_metadata_json" rows="4" placeholder="{}" style="width:100%;padding:8px;border:1px solid #ddd;border-radius:6px"></textarea>
      <div style="margin-top:12px;display:flex;gap:8px">
        <button type="submit" style="padding:8px 12px;border:1px solid #0ea5e9;border-radius:6px;background:#0ea5e9;color:#fff">Send</button>
        <button type="button" id="reset-post_units" style="padding:8px 12px;border:1px solid #ddd;border-radius:6px;background:#fff">Reset</button>
      </div>
    </form>
    <pre id="resp-post_units" style="margin-top:12px;background:#0b1020; color:#e2e8f0; padding:12px;border-radius:8px;overflow:auto"></pre>
  </div>
</Panel>

<script>
(function(){
  const form = document.getElementById('f-post_units');
  const out = document.getElementById('resp-post_units');
  const resetBtn = document.getElementById('reset-post_units');
  if(!form||!out) return;
  resetBtn && resetBtn.addEventListener('click', ()=>{ form.reset(); out.textContent=''; });
  form.addEventListener('submit', async (e)=>{
    e.preventDefault();
    try {
      const body = {};
{ const v = document.getElementById("fld_created_at").value; if(v!=="" || false) body["created_at"] = (String)(v); }
{ const v = document.getElementById("fld_updated_at").value; if(v!=="" || false) body["updated_at"] = (String)(v); }
{ const v = document.getElementById("fld_unit_id").value; if(v!=="" || true) body["unit_id"] = (String)(v); }
{ const v = document.getElementById("fld_unit_name").value; if(v!=="" || true) body["unit_name"] = (String)(v); }
{ const v = document.getElementById("fld_lifesmart_id").value; if(v!=="" || true) body["lifesmart_id"] = (String)(v); }
{ const v = document.getElementById("fld_lifesmart_pass").value; if(v!=="" || true) body["lifesmart_pass"] = (String)(v); }
{ const v = document.getElementById("fld_lifesmart_usertoken_json").value.trim(); if(v){ try{ body["lifesmart_usertoken"] = JSON.parse(v) }catch(e){ console.warn("Invalid JSON for lifesmart_usertoken") } } }
{ const v = document.getElementById("fld_lifesmart_usertoken_expiry_json").value.trim(); if(v){ try{ body["lifesmart_usertoken_expiry"] = JSON.parse(v) }catch(e){ console.warn("Invalid JSON for lifesmart_usertoken_expiry") } } }
{ const v = document.getElementById("fld_metadata_json").value.trim(); if(v){ try{ body["metadata"] = JSON.parse(v) }catch(e){ console.warn("Invalid JSON for metadata") } } }
      const res = await fetch('https://handler-{env}-ae.roomi-services.com/units', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify(body)
      });
      const text = await res.text();
      try { out.textContent = JSON.stringify(JSON.parse(text), null, 2); } catch { out.textContent = text; }
    } catch(err) {
      out.textContent = String(err);
    }
  });
})();
</script>
