---
title: "Bill Checkout"
api: "POST /api/payments/bills/{billId}/checkout"
authMethod: none
openapi: "/openapi-specs/Roomi Services.json POST /api/payments/bills/{billId}/checkout"
---

Process payment for a specific bill.

## Path Parameters


| Name | Type | Required | Description |
| --- | --- | --- | --- |
| billId | string | Yes | Bill identifier |


## Headers


| Name | Required | Description |
| --- | --- | --- |
| Authorization | Yes | AWS4-HMAC-SHA256 signature |
| X-Amz-Content-Sha256 | Yes | SHA256 hash of the request body |
| X-Amz-Date | Yes | ISO8601 timestamp |


## Body

```json
{
  "cardId": "card-123",
  "amount": 150.5
}
```


<RequestExample>

```json Request Body
{
  "cardId": "card-123",
  "amount": 150.5
}
```

</RequestExample>


<ResponseExample>

```json Response
{
  "transactionId": "txn-123",
  "status": "success",
  "message": "Payment processed successfully"
}
```

</ResponseExample>


<!-- GeneratedFieldsUI:post_api_payments_bills_billid_checkout -->
<Panel>
  <div id="form-post_api_payments_bills_billid_checkout">
    <form id="f-post_api_payments_bills_billid_checkout" style="display:block;max-width:720px">
      <label style="display:block;margin:6px 0 2px">cardId *</label><input id="fld_cardId" type="text" placeholder="card-123" style="width:100%;padding:8px;border:1px solid #ddd;border-radius:6px">
<label style="display:block;margin:6px 0 2px">amount </label><input id="fld_amount" type="number" placeholder="150.5" style="width:100%;padding:8px;border:1px solid #ddd;border-radius:6px">
      <div style="margin-top:12px;display:flex;gap:8px">
        <button type="submit" style="padding:8px 12px;border:1px solid #0ea5e9;border-radius:6px;background:#0ea5e9;color:#fff">Send</button>
        <button type="button" id="reset-post_api_payments_bills_billid_checkout" style="padding:8px 12px;border:1px solid #ddd;border-radius:6px;background:#fff">Reset</button>
      </div>
    </form>
    <pre id="resp-post_api_payments_bills_billid_checkout" style="margin-top:12px;background:#0b1020; color:#e2e8f0; padding:12px;border-radius:8px;overflow:auto"></pre>
  </div>
</Panel>

<script>
(function(){
  const form = document.getElementById('f-post_api_payments_bills_billid_checkout');
  const out = document.getElementById('resp-post_api_payments_bills_billid_checkout');
  const resetBtn = document.getElementById('reset-post_api_payments_bills_billid_checkout');
  if(!form||!out) return;
  resetBtn && resetBtn.addEventListener('click', ()=>{ form.reset(); out.textContent=''; });
  form.addEventListener('submit', async (e)=>{
    e.preventDefault();
    try {
      const body = {};
{ const v = document.getElementById("fld_cardId").value; if(v!=="" || true) body["cardId"] = (String)(v); }
{ const v = document.getElementById("fld_amount").value; if(v!=="" || false) body["amount"] = (Number)(v); }
      const res = await fetch('https://services-{env}-ae.roomi-services.com/api/payments/bills/{billId}/checkout', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify(body)
      });
      const text = await res.text();
      try { out.textContent = JSON.stringify(JSON.parse(text), null, 2); } catch { out.textContent = text; }
    } catch(err) {
      out.textContent = String(err);
    }
  });
})();
</script>
