---
title: "Devices - List"
api: "GET /v1/devices"
authMethod: "none"
---

Retrieve all devices for a specific unit with their status and capabilities.

<Note>
  Include `x-unit-id` and `x-user-id` headers and sign with AWS SigV4. No `x-roomi-token`.
</Note>

