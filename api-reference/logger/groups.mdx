---
title: "Log Groups"
api: "POST /groups"
authMethod: none
openapi: "/openapi-specs/Roomi Logger.json POST /groups"
---

Create a log group. Use GET /groups to list groups and DELETE /groups/{group_name} to remove.

## Headers


| Name | Required | Description |
| --- | --- | --- |
| Authorization | Yes | AWS4-HMAC-SHA256 signature |
| X-Amz-Content-Sha256 | Yes | SHA256 hash of the request body |
| X-Amz-Date | Yes | ISO8601 timestamp |


## Body

```json
{
  "name": "roomi/app",
  "retention_days": 30,
  "tags": {
    "env": "dev"
  }
}
```


<RequestExample>

```json Request Body
{
  "name": "name",
  "tags": {}
}
```

</RequestExample>


<ResponseExample>

```json Response
{
  "message": "message"
}
```

</ResponseExample>


<!-- GeneratedFieldsUI:post_groups -->
<Panel>
  <div id="form-post_groups">
    <form id="f-post_groups" style="display:block;max-width:720px">
      <label style="display:block;margin:6px 0 2px">name *</label><input id="fld_name" type="text" placeholder="name" style="width:100%;padding:8px;border:1px solid #ddd;border-radius:6px">
<label style="display:block;margin:6px 0 2px">retention_days (JSON)</label><textarea id="fld_retention_days_json" rows="4" placeholder="{}" style="width:100%;padding:8px;border:1px solid #ddd;border-radius:6px"></textarea>
<label style="display:block;margin:6px 0 2px">tags (JSON)</label><textarea id="fld_tags_json" rows="4" placeholder="{}" style="width:100%;padding:8px;border:1px solid #ddd;border-radius:6px"></textarea>
      <div style="margin-top:12px;display:flex;gap:8px">
        <button type="submit" style="padding:8px 12px;border:1px solid #0ea5e9;border-radius:6px;background:#0ea5e9;color:#fff">Send</button>
        <button type="button" id="reset-post_groups" style="padding:8px 12px;border:1px solid #ddd;border-radius:6px;background:#fff">Reset</button>
      </div>
    </form>
    <pre id="resp-post_groups" style="margin-top:12px;background:#0b1020; color:#e2e8f0; padding:12px;border-radius:8px;overflow:auto"></pre>
  </div>
</Panel>

<script>
(function(){
  const form = document.getElementById('f-post_groups');
  const out = document.getElementById('resp-post_groups');
  const resetBtn = document.getElementById('reset-post_groups');
  if(!form||!out) return;
  resetBtn && resetBtn.addEventListener('click', ()=>{ form.reset(); out.textContent=''; });
  form.addEventListener('submit', async (e)=>{
    e.preventDefault();
    try {
      const body = {};
{ const v = document.getElementById("fld_name").value; if(v!=="" || true) body["name"] = (String)(v); }
{ const v = document.getElementById("fld_retention_days_json").value.trim(); if(v){ try{ body["retention_days"] = JSON.parse(v) }catch(e){ console.warn("Invalid JSON for retention_days") } } }
{ const v = document.getElementById("fld_tags_json").value.trim(); if(v){ try{ body["tags"] = JSON.parse(v) }catch(e){ console.warn("Invalid JSON for tags") } } }
      const res = await fetch('https://logger-{env}-ae.roomi-services.com/groups', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify(body)
      });
      const text = await res.text();
      try { out.textContent = JSON.stringify(JSON.parse(text), null, 2); } catch { out.textContent = text; }
    } catch(err) {
      out.textContent = String(err);
    }
  });
})();
</script>
