---
title: "Get Current User"
api: "GET /api/me"
authMethod: none
openapi: "/openapi-specs/Roomi Auth.json GET /api/me"
---

Get current authenticated user information.

## Headers


| Name | Required | Description |
| --- | --- | --- |
| Authorization | Yes | AWS4-HMAC-SHA256 signature |
| X-Amz-Content-Sha256 | Yes | SHA256 hash of the request body |
| X-Amz-Date | Yes | ISO8601 timestamp |



<RequestExample>

```json Request Body
{
  "phone": "+971501234567",
  "unit_id": "unit-123"
}
```

</RequestExample>


<ResponseExample>

```json Response
{
  "user_id": "user-123",
  "phone": "+971501234567",
  "is_registered": true,
  "units": []
}
```

</ResponseExample>


<!-- GeneratedFieldsUI:get_api_me -->
<Panel>
  <div id="form-get_api_me">
    <form id="f-get_api_me" style="display:block;max-width:720px">
      <label style="display:block;margin:6px 0 2px">phone </label><input id="fld_phone" type="text" placeholder="+971501234567" style="width:100%;padding:8px;border:1px solid #ddd;border-radius:6px">
<label style="display:block;margin:6px 0 2px">unit_id </label><input id="fld_unit_id" type="text" placeholder="unit-123" style="width:100%;padding:8px;border:1px solid #ddd;border-radius:6px">
      <div style="margin-top:12px;display:flex;gap:8px">
        <button type="submit" style="padding:8px 12px;border:1px solid #0ea5e9;border-radius:6px;background:#0ea5e9;color:#fff">Send</button>
        <button type="button" id="reset-get_api_me" style="padding:8px 12px;border:1px solid #ddd;border-radius:6px;background:#fff">Reset</button>
      </div>
    </form>
    <pre id="resp-get_api_me" style="margin-top:12px;background:#0b1020; color:#e2e8f0; padding:12px;border-radius:8px;overflow:auto"></pre>
  </div>
</Panel>

<script>
(function(){
  const form = document.getElementById('f-get_api_me');
  const out = document.getElementById('resp-get_api_me');
  const resetBtn = document.getElementById('reset-get_api_me');
  if(!form||!out) return;
  resetBtn && resetBtn.addEventListener('click', ()=>{ form.reset(); out.textContent=''; });
  form.addEventListener('submit', async (e)=>{
    e.preventDefault();
    try {
      const body = {};
{ const v = document.getElementById("fld_phone").value; if(v!=="" || false) body["phone"] = (String)(v); }
{ const v = document.getElementById("fld_unit_id").value; if(v!=="" || false) body["unit_id"] = (String)(v); }
      const res = await fetch('https://auth-{env}-ae.roomi-services.com/api/me', {
        method: 'GET',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify(body)
      });
      const text = await res.text();
      try { out.textContent = JSON.stringify(JSON.parse(text), null, 2); } catch { out.textContent = text; }
    } catch(err) {
      out.textContent = String(err);
    }
  });
})();
</script>
